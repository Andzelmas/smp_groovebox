================================================================================
CROSS-CUTTING INVARIANT — identity/storage rule for anything CX-backed
================================================================================
The CX layer resolves a ContextId to a module's raw user_data pointer ONCE
and reuses that SAME pointer across many later calls, without re-resolving
it each time. So: a fixed-array/pointer-backed module must NEVER move a
surviving item's storage once a ContextId has been handed out for it - no
compaction on remove. Free slots become gaps/tombstones, reused only by a
later NEW item (which gets a new identity). Already how samples/plugins
work (contexts/sampler.c, app_data.c's ops tables); params' own resync
(STAGE 2 below) must follow the same rule.

================================================================================
STAGE 2 — dynamic params (add/remove at runtime), PARAMS RESYNC
================================================================================
Not built yet. contexts/clap_plugins.c's clap_plug_ext_params_rescan
handles CLAP_PARAM_RESCAN_VALUES/TEXT/INFO for real; its CLAP_PARAM_
RESCAN_ALL branch (param count/order/ids changed) is still a TODO stub
that only logs. Do NOT implement it as "destroy the container and
rebuild" - that hands every surviving param a brand-new identity even
when nothing about it changed, breaking the CROSS-CUTTING INVARIANT.

TODO: implement params_container_resync(container, new_params[],
new_count), called by the owner (CLAP's RESCAN_ALL handler) with its
current full param list, matched by uid not position:
  pass 1 (removals) - for each val_id whose current uid is no longer in
    new_params: free(rt_params[val_id]); rt_params[val_id] = NULL; same
    for ui_params[val_id]. Nothing else moves; num_of_params_rt/ui do NOT
    shrink - the slot stays a valid, NULL, reusable index. NEVER compact/
    shift surviving params down to close the gap (would silently
    reassign val_id for every survivor after it - breaks PARAM_RING_
    DATA_BIT messages in flight and any other code that resolved a
    val_id and expects it to keep meaning the same param). Shrinking the
    outer array's CAPACITY by realloc'ing away trailing NULLs (tail
    only, never a middle gap) is safe if ever worth it - not needed yet.
  pass 2 (additions) - for each new_params[i] with no matching alive
    (non-NULL) slot: find the first NULL slot (scan rt_params; ui_params
    is NULL at the same index in lockstep) and malloc fresh PRM_PARAM_RT/
    PRM_PARAM_UI into it, same field population param_add_param already
    does (PRM_PARAM_RT: val, min_val, max_val, val_changed=0, uid,
    owner_id, cookie; PRM_PARAM_UI: val, min_val, max_val, def_val=val,
    inc_am, name, uid, owner_id, is_hidden=0) - uid minted from CLAP_PLUG_
    PLUG.next_param_uid, which must NOT be reset here (only clap_plug_
    params_create resets it, at initial discovery). No NULL slot found ->
    grow both outer arrays by one (realloc).
  survivors (uid in both) - untouched by either pass; RESCAN_VALUES/INFO
    already refresh value/name for these.
  Safe to mutate both arrays directly, no ring-buffer messaging needed
  for the structural part - RESCAN_ALL only fires while the plugin is
  deactivated (`if (plug->plug_inst_activated == 1) return;`), so the rt
  thread isn't touching this container while resync runs.
  Reports nothing to anyone - STAGE 3's own CX resync (below) discovers
  what changed later by re-walking child_at and diffing.

TODO: something to walk CLAP's CURRENT index space fresh (0..count())
after a RESCAN_ALL and build the new_params[] array resync takes as
input (name, min, max, inc, uid=next_param_uid++, owner_id=param_info.id,
cookie, per current index) - structurally identical to clap_plug_params_
create's own discovery loop. Factor both into one shared helper rather
than duplicating it.

TODO: every accessor in params.c needs a NULL check on the val_id it
dereferences, once tombstoning exists - today they all assume every
index below num_of_params_rt/ui is non-NULL. Applies to: param_get_if_
any_changed_rt's loop, param_find_uid's loop, param_msgs_process (the
one slot it writes into - needs the guard before its uid comparison can
even run), and any future params-as-CX child_at. Anything walking
0..num_of_params needs to SKIP NULL slots (not stop), same as sampler.c's
own child_at already does for its samples array's gaps.

If LV2 ever needs the same resync (it doesn't today - plug->controls is
fixed at load, no add/remove-ports mechanism in LV2 core or the patch/
parameters/port-props extensions this app uses): uid would need to stop
being ct_iter (position) and become the port/control's SYMBOL hashed into
a uint32_t instead - position is exactly what goes unstable across a
reconfiguration. Not automatic, a deliberate plugins.c change.

Once resync exists: revisit whether STAGE 3's CX-child wiring needs any
of its growth logic duplicated, or can call params_container_resync
directly.

================================================================================
STAGE 3 — parameters as CX contexts, integrated into the action system
================================================================================
Agreed direction: params become CX tree nodes (like plugin instances),
not a live external list (like JACK ports).

TODO / open question:
- Materialize every parameter as a CX child, or only non-hidden ones
  (PRM_PARAM_UI.is_hidden already exists)? Check real per-plugin param
  counts (tens to low hundreds possible) against how the rest of the CX
  tree scales before committing.

TODO, shape already decided:
- Value changes: go through the (ui-side) param_set_value and its
  existing ring-buffer message to rt - matches the JACK/plugins pattern,
  no new plumbing expected.
- Params as CX children: sample_ops/lv2_plugin_ops/clap_plugin_ops/synth-
  osc ops currently stub child_at to `return false` - need real child_at/
  child_count backed by PRM_CONTAIN (needs STAGE 2 first). Each owning
  instance needs its own instance-scoped params_dirty bool (distinct from
  the container-level plugins_dirty/samples_dirty, which mean "the SET of
  instances changed"), set right after params_container_resync finishes
  and checked inside the existing per-cycle rt-to-ui message loop
  (clap_read_rt_to_ui_messages today; LV2/synth/sampler need the
  equivalent if/when they get dynamic params too). The rest of the
  pipeline (child diff, DATA_EVENT_CHILDREN_CHANGED) is already fully
  generic - zero new code needed there.
- DATA_ACTION_SET_VALUE (one numeric arg, the value) and DATA_ACTION_
  ADJUST_VALUE (one signed step COUNT, not a raw value - action_do maps
  sign to Operation_Increase/Decrease). Both map to one-line param_set_
  value(container, val_id, set_to, NULL, Operation_...) calls in
  action_do. Other paramOperType values (DefValue/SetIncr/SetDefValue/
  ChangeName/ToggleHidden) stay unexposed to the action system until
  something needs them.
- DATA_ACTION_SET_CHOICE for CLAP_PARAM_IS_ENUM params: reuse list_at/
  DataChoice like the plugin catalogue does - list_at walks min..max
  calling param_get_value_as_string per step for the label. Reuses
  helper_choice_list_step as-is on the CLI side. Do NOT make the UI loop
  min..max itself (pushes owner-specific domain knowledge into the UI).
- Param categories: PRM_PARAM_UI gets a category/module string field
  (CLAP: clap_param_info_t.module). Params stay FLAT CX children of their
  owning instance - category is presentational metadata only, not a real
  tree level. child_at should return params sorted by category so a UI
  can print a header over contiguous runs without a real tree node. Add
  the field + an accessor when actually wiring params into a UI.

TODO - gaps found checking clap_plugins.c against the vendored clap/ext/
params.h, still open:
- CLAP_PARAM_IS_READONLY already zeroes inc_am but doesn't disable
  anything - SET_VALUE/ADJUST_VALUE/SET_CHOICE's DataAction.enabled
  should gate on it once those actions exist.
- CLAP_PARAM_IS_BYPASS: still an empty TODO stub, exposed as an ordinary
  param for now - decide later whether it deserves dedicated treatment.
- clap_output_events_read never parses a CLAP_EVENT_PARAM_VALUE the
  plugin itself sends (own TODO: "need to find a plugin that outputs
  them") - the plugin's own internal value changes never reach us.
- Gesture begin/end (CLAP_EVENT_PARAM_GESTURE_BEGIN/_END): SET_VALUE/
  ADJUST_VALUE would send a bare PARAM_VALUE with no gesture wrapping -
  unverified against a plugin that keys behaviour off gesture end.
- clap_host_params.request_flush()/flush(): matters if a plugin is
  loaded/active while process() isn't being called. Not audited.
- Automation/modulation (IS_AUTOMATABLE*/IS_MODULATABLE*, PER_NOTE_ID/
  PER_KEY/PER_CHANNEL/PER_PORT, CLAP_EVENT_PARAM_MOD): deliberately out
  of scope - automation needs a recorded-timeline/playback engine this
  app doesn't have; modulation would compose with a routing action
  related to but not reducible to CONNECT. Revisit only when specifically
  decided to build either.

================================================================================
FUTURE - categories for CHOICE / MULTI_CHOICE lists (not scheduled -
needed once plugin preset browsers are built)
================================================================================
No new DataListId/DataArgKind/DataActionType needed - still a flat,
idx-enumerable list_at. TODO when built:
- DataChoice gains DATA_CHOICE_CATEGORY (1u << 1), alongside the existing
  DATA_CHOICE_LINKED (1u << 0) - a row with this bit set is a node to
  descend into, not a pickable leaf.
- DataActionReq gains a top-level field, sibling to `type`, OUTSIDE the
  per-action union: `uint64_t category_scope;` (0 = list root). Set by
  the UI while drilling; ignored by list_at for non-categorised lists.
  Must be a dedicated top-level field, not a reused union field (e.g. NOT
  add_choice.choice_value - collides with CONNECT's own union usage).
- CLI-side drilling: one more branch in the existing "on SELECTED"
  handling already used by helper_choice_list_step's callers - category
  row updates category_scope/resets cursor and re-browses; leaf row
  behaves exactly as today. Backing out (h/ESC) needs a small caller-
  owned breadcrumb stack (scope, cursor) per level, capped like ACTION_
  ARG_COUNT elsewhere in this CLI - no heap.
- A category node's DataChoice.value must hash its FULL path (e.g.
  fnv1a64("Bass/Analog/Warm")), not just its own name, or two different
  parents with a same-named child collide.
- Only the immediate parent's scope value is ever needed by list_at, not
  the whole ancestor chain.
